/**
 * @title         Marker Type
 * @description   Enumeration of the possible types of vector line endings
 **/
export enum MarkerType {
  OpenArrow = 0,
  FilledArrow = 1,
  Line = 2,
  OpenCircle = 3,
  FilledCircle = 4,
  OpenSquare = 5,
  FilledSquare = 6
}